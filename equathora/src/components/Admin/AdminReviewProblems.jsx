import React from 'react'

const AdminReviewProblems = () => {
    return (
        <div>
            {/* TODO: show list of possible new problems */}
            {/* Add visual elements to understand if it has been published yet
                if it has already been published it gets a bit hidden but can be still reviewed etc */}
            {/* A button to copy all of them at once
            When you click one of them it opens and shows all the other details
            Other functionalities of your liking */}
        </div>
    )
}

export default AdminReviewProblems
